FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt pyproject.toml ./
COPY src/ ./src/

RUN pip install --no-cache-dir -r requirements.txt && \
    pip install --no-cache-dir .

RUN mkdir -p /app/logs

ENV PYTHONPATH=/app

CMD ["python", "-m", "src.scripts.inference"]